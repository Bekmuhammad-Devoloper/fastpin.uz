# Dev server proxies API calls to a LOCAL backend
# (almaz on :4000 -> local Postgres on :5434).
# Comment this out to test against production (https://api.fastpin.uz) instead.
VITE_PROXY_TARGET=http://localhost:4000

# Empty = axios uses relative paths, so the vite proxy above does the routing.
VITE_API_URL=
